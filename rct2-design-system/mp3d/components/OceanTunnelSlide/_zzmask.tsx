// ---------------------------------------------------------------------------
// TOMBSTONE — this file is not part of the design system.
//
// It was an A/B MUTATION COPY of OceanTunnelSlide/index.tsx: byte-identical
// except one line, where the spit mask was removed to see what that changed.
// Scratch, referenced by nothing, and it should never have been pushed — a
// 76 KB near-duplicate sitting in `components/` is in the catalog's path, so
// `list_components` can surface it and a generating park can install and mount
// a second, subtly-wrong OceanTunnelSlide.
//
// Magic Patterns has no delete-file tool, so the content is replaced instead of
// removed. Do not restore it. If that experiment needs re-running, use
// `harness/mp3d-render/mutate-setpiece.mjs`, which builds its mutant into a
// scratch directory and deletes it afterwards for exactly this reason.
//
// USE `./index.tsx`.
// ---------------------------------------------------------------------------
export {};
