import React from 'react';
import { Swans } from './index';

const previews = {
  componentName: 'Swans',
  importPath: 'components/Swans',
  previews: [
    { name: 'Default', description: "Swans \u2014 real RCT2 asset across its 4 map rotations.", render: () => <Swans /> },
  ],
};

export default previews;
