import React from 'react';
import { ParkBench } from './index';

const previews = {
  componentName: 'ParkBench',
  importPath: 'components/ParkBench',
  previews: [
    { name: 'Default', description: "Park Bench \u2014 real RCT2 asset across its 4 map rotations.", render: () => <ParkBench /> },
  ],
};

export default previews;
