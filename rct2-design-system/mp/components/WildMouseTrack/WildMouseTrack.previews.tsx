import React from 'react';
import { WildMouseTrack } from './index';

const previews = {
  componentName: 'WildMouseTrack',
  importPath: 'components/WildMouseTrack',
  previews: [
    { name: 'Default', description: "Wild Mouse Track \u2014 real RCT2 asset across its 4 map rotations.", render: () => <WildMouseTrack /> },
  ],
};

export default previews;
