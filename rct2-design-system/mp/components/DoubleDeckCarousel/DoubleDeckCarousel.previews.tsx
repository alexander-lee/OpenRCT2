import React from 'react';
import { DoubleDeckCarousel } from './index';

const previews = {
  componentName: 'DoubleDeckCarousel',
  importPath: 'components/DoubleDeckCarousel',
  previews: [
    { name: 'Default', description: "Double Deck Carousel \u2014 real RCT2 asset across its 4 map rotations.", render: () => <DoubleDeckCarousel /> },
  ],
};

export default previews;
